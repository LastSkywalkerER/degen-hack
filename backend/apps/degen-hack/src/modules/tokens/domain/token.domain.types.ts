export interface CreateToken {
  name: string;
  tokenProxy: string;
  symbol: string;
  initialPrice: number;
  logo: string;
  burnPercentage: number;
  yeetRate: number;
  transactionHash: string;
}
